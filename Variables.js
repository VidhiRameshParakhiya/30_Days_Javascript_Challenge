// VIDHI RAMESH PARAKHIYA
// 30 DAYS CHALLENGE

// Activity1 Variable Declaration
var numbers=123;
let name="vidhiRameshParakhiya"
console.log(numbers,name)

// Activity2 Constant declaration
const boolean=Boolean=5>3;
console.table([numbers,name,boolean]);

// Activity3 Data types
var Numbers=9865623562;
let letters="itsmyname";
let bool=Boolean==6<3;
let object={name:"vidhi",bestfriend:"deepali"}
let array=[1,2,3]
console.log(typeof array,Numbers,letters,bool,object);

// Activity4
let year=856;
year=988;
console.log('let year is=',year)

// Activity5
const years=123;
// v=666; cannot reassign const value
console.log('const years is=',years);

// NOTES
// We can change value of let from memory but
// we cannot change value of const,once its assign it cannot change
// Example-Before in js we used to use var but if one developers change one name 
// value than wherever there is name keyword, everthing gets Change.so we use const and let more 
// avoid using var.